[project]
name = "computational-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "numpy>=2.4.6",
    "pydantic>=2.13.4",
    "pytest>=9.0.3",
    "uvicorn>=0.47.0",
    "tenseal>=0.3.15"
]
tool.uv.environments = [
    "sys_platform == 'linux' and platform_machine == 'amd64'"
]
required-environments = [
    "sys_platform == 'linux' and platform_machine == 'amd64'"
]
